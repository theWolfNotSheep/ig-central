spring:
  rabbitmq:
    host: rabbitmq
    port: 5672

minio:
  endpoint: http://minio:9000

pipeline:
  api-base-url: http://api:8080
  execution-engine:
    enabled: ${PIPELINE_EXECUTION_ENGINE_ENABLED:false}
