# Base docker-compose — all services including cloudflared tunnel:
#   docker compose up --build -d
#   Requires TUNNEL_TOKEN in .env for cloudflared to connect

x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-cloudflared
    restart: unless-stopped
    command: tunnel --no-autoupdate run --token ${TUNNEL_TOKEN}
    depends_on:
      nginx:
        condition: service_healthy
    logging: *default-logging
    networks:
      - gls

  mongo:
    image: mongo:7
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-mongo
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD:-example}
    volumes:
      - ./data/mongo:/data/db
    healthcheck:
      test: ["CMD", "mongosh", "--eval", "db.adminCommand('ping')"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    logging: *default-logging
    networks:
      - gls

  minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:-minioadmin}
    volumes:
      - ./data/minio:/data
    ports:
      - "9000:9000"
      - "9001:9001"
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 20s
    logging: *default-logging
    networks:
      - gls

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.17.0
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-elasticsearch
    restart: unless-stopped
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    volumes:
      - ./data/elasticsearch:/usr/share/elasticsearch/data
    ports:
      - "9200:9200"
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:9200/_cluster/health"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    logging: *default-logging
    networks:
      - gls

  rabbitmq:
    image: rabbitmq:4-management-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-rabbitmq
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASSWORD:-guest}
      # LLM classification calls can take several minutes; disable consumer delivery ACK timeout
      # to prevent RabbitMQ from killing channels mid-classification.
      RABBITMQ_SERVER_ADDITIONAL_ERL_ARGS: "-rabbit consumer_timeout 3600000"
    volumes:
      - ./data/rabbitmq:/var/lib/rabbitmq
    ports:
      - "15672:15672"
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "check_running"]
      interval: 15s
      timeout: 10s
      retries: 5
      start_period: 30s
    logging: *default-logging
    networks:
      - gls

  bert-classifier:
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-bert-classifier
    restart: unless-stopped
    build:
      context: ./bert-classifier
      dockerfile: Dockerfile
    environment:
      BERT_MODEL_DIR: /app/models/default
      BERT_LABEL_MAP: /app/models/default/label_map.json
      BERT_USE_ONNX: "true"
      BERT_MAX_LENGTH: "256"
    deploy:
      resources:
        limits:
          memory: 4g
    volumes:
      - ./data/bert-models:/app/models
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=5)"]
      interval: 30s
      timeout: 15s
      retries: 10
      start_period: 30s
    logging: *default-logging
    networks:
      - gls

  api:
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-api
    restart: unless-stopped
    build:
      # Repo-root context so the build can reach contracts/ —
      # gls-platform-audit bundles the audit envelope schema from
      # contracts/audit/ at build time, and that resource path goes up
      # two levels from the module's pom.
      context: .
      dockerfile: backend/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SERVER_PORT: "8080"
      APP_ENV: ${APP_ENV:-DEV}
      PUBLIC_URL: ${PUBLIC_URL}
      BACKEND_BASE_URL: ${BACKEND_BASE_URL}
      EMAIL_SMTP_HOST: ${EMAIL_SMTP_HOST:-smtp.office365.com}
      EMAIL_SMTP_PORT: ${EMAIL_SMTP_PORT:-587}
      EMAIL_ADDRESS: ${EMAIL_ADDRESS}
      EMAIL_PASSWORD: ${EMAIL_PASSWORD}
      ADMIN_EMAIL: ${ADMIN_EMAIL:-admin@governanceledstore.co.uk}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      JWT_SECRET: ${JWT_SECRET:-}
      LINKEDIN_CLIENT_ID: ${LINKEDIN_CLIENT_ID:-}
      LINKEDIN_CLIENT_SECRET: ${LINKEDIN_CLIENT_SECRET:-}
      GIT_HUB_CLIENT_ID: ${GIT_HUB_CLIENT_ID:-}
      GIT_HUB_CLIENT_SECRET: ${GIT_HUB_CLIENT_SECRET:-}
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-gls-documents}
      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200
      # Google OAuth, Hub connection, and LLM config come from Settings UI (app_config in MongoDB)
      # These are fallback defaults only
      # Hub runs as a separate stack — point at its public URL (override via Settings UI)
      GOVERNANCE_HUB_URL: ${GOVERNANCE_HUB_URL:-https://hub.igcentral.com}
      GOVERNANCE_HUB_API_KEY: ""
      GOOGLE_OAUTH_CLIENT_ID: ""
      GOOGLE_OAUTH_CLIENT_SECRET: ""
      GOOGLE_OAUTH_REDIRECT_URI: ${PUBLIC_URL}/api/drives/google/callback
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      PIPELINE_BERT_SERVICE_URL: http://bert-classifier:8000
      LLM_SERVICE_URL: http://llm-worker:8082
      LLM_TIMEOUT_MS: "300000"
    depends_on:
      mongo:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8080/actuator/info"]
      interval: 20s
      timeout: 10s
      retries: 10
      start_period: 120s
    logging: *default-logging
    networks:
      - gls

  mcp-server:
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-mcp-server
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/Dockerfile.mcp
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SERVER_PORT: "8081"
      PIPELINE_API_BASE_URL: http://api:8080
    depends_on:
      mongo:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8081/actuator/info"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging: *default-logging
    networks:
      - gls

  llm-worker:
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-llm-worker
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/Dockerfile.llm
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SERVER_PORT: "8082"
      # LLM config comes from Settings UI (app_config in MongoDB)
      # These are fallback defaults only — used if no config exists in DB
      ANTHROPIC_API_KEY: ""
      LLM_PROVIDER: anthropic
      OLLAMA_BASE_URL: http://host.docker.internal:11434
      OLLAMA_MODEL: qwen2.5:32b
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_USER: ${RABBITMQ_USER:-guest}
      RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      MCP_SERVER_URL: http://mcp-server:8081
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-gls-documents}
    depends_on:
      mongo:
        condition: service_healthy
      rabbitmq:
        condition: service_healthy
      minio:
        condition: service_healthy
      mcp-server:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:8082/actuator/health"]
      interval: 20s
      timeout: 10s
      retries: 5
      start_period: 60s
    logging: *default-logging
    networks:
      - gls

  web:
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-web
    restart: unless-stopped
    build:
      context: ./web
      dockerfile: Dockerfile
      args:
        BACKEND_BASE_URL: http://api:8080
        NEXT_PUBLIC_APP_ENV: ${APP_ENV:-DEV}
    environment:
      BACKEND_BASE_URL: http://api:8080
      NODE_ENV: production
      PORT: "3000"
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s
    logging: *default-logging
    networks:
      - gls

  nginx:
    image: nginx:alpine
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    depends_on:
      web:
        condition: service_healthy
      api:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://127.0.0.1:80"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    logging: *default-logging
    networks:
      - gls

  # ── Governance Hub ──────────────────────────────────────
  # The hub runs as a standalone stack on its own network.
  # See hub/docker-compose.yml and hub/README.md.

  prometheus:
    image: prom/prometheus:latest
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-prometheus
    restart: unless-stopped
    volumes:
      - ./monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
    ports:
      - "9090:9090"
    networks:
      - gls

  grafana:
    image: grafana/grafana:latest
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-grafana
    restart: unless-stopped
    ports:
      - "3003:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - ./monitoring/datasources:/etc/grafana/provisioning/datasources:ro
      - ./monitoring/dashboards:/etc/grafana/provisioning/dashboards:ro
      - grafana_data:/var/lib/grafana
    depends_on:
      - prometheus
    networks:
      - gls

  # ====================================================================
  # v2 Phase 1+ services — placeholders.
  #
  # These are the services that land as the v2 cutover progresses. Each
  # block stays commented out until its phase begins; uncomment + fill in
  # when the corresponding `gls-<service>/` Maven module + Dockerfile
  # exists. The shape below is the cloneable pattern from
  # `docs/service-template.md` (lands with Phase 0.5 reference impl).
  # See `version-2-implementation-plan.md` for phase ownership.
  # ====================================================================

  # gls-extraction-tika:                                  # Phase 0.5
  #   container_name: ${COMPOSE_PROJECT_NAME:-gls}-extraction-tika
  #   restart: unless-stopped
  #   build:
  #     # Repo-root context so the build can reach contracts/ — the
  #     # gls-platform-audit Maven module bundles the audit envelope
  #     # schema from contracts/audit/ at build time.
  #     context: .
  #     dockerfile: backend/gls-extraction-tika/Dockerfile
  #   environment:
  #     SPRING_PROFILES_ACTIVE: docker
  #     MINIO_ENDPOINT: http://minio:9000
  #     MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
  #     MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
  #     # Tika service is currently stateless — no Mongo / Rabbit needs
  #     # until idempotency + audit emission land. Add them then.
  #   depends_on:
  #     minio: { condition: service_healthy }
  #   logging: *default-logging
  #   networks: [gls]

  gls-extraction-archive:                                 # Phase 1.1
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-extraction-archive
    restart: unless-stopped
    build:
      # Repo-root context (see Tika block above) so contracts/ is reachable.
      context: .
      dockerfile: backend/gls-extraction-archive/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-guest}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      GLS_ARCHIVE_SINK_BUCKET: ${GLS_ARCHIVE_SINK_BUCKET:-gls-archive-children}
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      minio: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-extraction-ocr:                                     # Phase 1.1
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-extraction-ocr
    restart: unless-stopped
    build:
      # Repo-root context. OCR_LANGUAGES build-arg can be set via
      # `docker compose build --build-arg OCR_LANGUAGES="eng fra deu"`.
      context: .
      dockerfile: backend/gls-extraction-ocr/Dockerfile
      args:
        OCR_LANGUAGES: ${OCR_LANGUAGES:-eng}
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-guest}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      GLS_OCR_SINK_BUCKET: ${GLS_OCR_SINK_BUCKET:-gls-ocr-text}
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      minio: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-extraction-audio:                                   # Phase 1.1
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-extraction-audio
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-extraction-audio/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-guest}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      GLS_AUDIO_PROVIDER: ${GLS_AUDIO_PROVIDER:-none}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GLS_AUDIO_SINK_BUCKET: ${GLS_AUDIO_SINK_BUCKET:-gls-audio-text}
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      minio: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-classifier-router:                                  # Phase 1.2
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-classifier-router
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-classifier-router/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_RABBITMQ_HOST: rabbitmq
      SPRING_RABBITMQ_USERNAME: ${RABBITMQ_USER:-guest}
      SPRING_RABBITMQ_PASSWORD: ${RABBITMQ_PASSWORD:-guest}
      # Cascade tier feature flags. Default off — flip to true once
      # the targeted worker is reachable.
      GLS_ROUTER_BERT_ENABLED: ${GLS_ROUTER_BERT_ENABLED:-false}
      GLS_ROUTER_BERT_URL: http://gls-bert-inference:8094
      GLS_ROUTER_SLM_ENABLED: ${GLS_ROUTER_SLM_ENABLED:-false}
      GLS_ROUTER_SLM_URL: http://gls-slm-worker:8095
      # Legacy Rabbit-based LLM dispatch (gls-llm-orchestration).
      # Phase 1.6 PR3 introduced an HTTP path; flip GLS_ROUTER_LLM_HTTP_ENABLED
      # to use it. When both are on, HTTP wins.
      GLS_ROUTER_LLM_ENABLED: ${GLS_ROUTER_LLM_ENABLED:-false}
      GLS_ROUTER_LLM_HTTP_ENABLED: ${GLS_ROUTER_LLM_HTTP_ENABLED:-false}
      GLS_ROUTER_LLM_HTTP_URL: http://gls-llm-worker:8096
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-bert-inference:                                     # Phase 1.4
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-bert-inference
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-bert-inference/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      # Default: stub backend that returns MODEL_NOT_LOADED 503.
      # Flip GLS_BERT_ENGINE to `djl` once the trainer publishes its
      # first ONNX artefact + the engine wires in.
      GLS_BERT_ENGINE: ${GLS_BERT_ENGINE:-none}
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      GLS_BERT_ARTIFACTS_BUCKET: ${GLS_BERT_ARTIFACTS_BUCKET:-gls-bert-artifacts}
    depends_on:
      minio: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-slm-worker:                                         # Phase 1.5
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-slm-worker
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-slm-worker/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      # Default: stub backend that returns SLM_NOT_CONFIGURED 503.
      # Flip GLS_SLM_BACKEND to `anthropic` (with ANTHROPIC_API_KEY)
      # or `ollama` (with OLLAMA_BASE_URL) to enable real classification.
      GLS_SLM_BACKEND: ${GLS_SLM_BACKEND:-none}
      SPRING_AI_ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      SPRING_AI_OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      GLS_SLM_ANTHROPIC_MODEL: ${GLS_SLM_ANTHROPIC_MODEL:-claude-haiku-4-5}
      GLS_SLM_OLLAMA_MODEL: ${GLS_SLM_OLLAMA_MODEL:-llama3.1:8b}
      GLS_MCP_SERVER_URL: http://mcp-server:8081
    depends_on:
      mongo: { condition: service_healthy }
      mcp-server: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-llm-worker:                                         # Phase 1.6
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-llm-worker
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-llm-worker/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      # Default: stub backend that returns LLM_NOT_CONFIGURED 503.
      # Flip GLS_LLM_BACKEND to `anthropic` (with ANTHROPIC_API_KEY)
      # or `ollama` (with OLLAMA_BASE_URL) to enable real classification.
      GLS_LLM_BACKEND: ${GLS_LLM_BACKEND:-none}
      SPRING_AI_ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      SPRING_AI_OLLAMA_BASE_URL: ${OLLAMA_BASE_URL:-http://host.docker.internal:11434}
      GLS_LLM_ANTHROPIC_MODEL: ${GLS_LLM_ANTHROPIC_MODEL:-claude-sonnet-4-5}
      GLS_LLM_OLLAMA_MODEL: ${GLS_LLM_OLLAMA_MODEL:-qwen2.5:32b}
      GLS_MCP_SERVER_URL: http://mcp-server:8081
      # Cost budget + rate limit defaults are 0 (disabled). Tune via
      # GLS_LLM_BUDGET_DAILY_TOKEN_CAP / GLS_LLM_RATE_LIMIT_PERMITS.
      GLS_LLM_BUDGET_DAILY_TOKEN_CAP: ${GLS_LLM_BUDGET_DAILY_TOKEN_CAP:-0}
      GLS_LLM_RATE_LIMIT_PERMITS: ${GLS_LLM_RATE_LIMIT_PERMITS:-0}
      GLS_LLM_RATE_LIMIT_WAIT_MS: ${GLS_LLM_RATE_LIMIT_WAIT_MS:-0}
    depends_on:
      mongo: { condition: service_healthy }
      mcp-server: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-enforcement-worker:                                 # Phase 1.10
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-enforcement-worker
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-governance-enforcement/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      MINIO_ENDPOINT: http://minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:-minioadmin}
      MINIO_BUCKET: ${MINIO_BUCKET:-gls-documents}
      # The HTTP worker is the new entry point (Phase 1.10 PR3 will
      # cut the orchestrator over to it). Disable the legacy in-process
      # Rabbit consumer in this container so it doesn't double-process
      # the classified queue alongside the api/execution-engine path.
      # The consumer is `@ConditionalOnProperty(... matchIfMissing=true,
      # havingValue=false)` — set the flag to `true` to disable it.
      PIPELINE_EXECUTION_ENGINE_ENABLED: "true"
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      minio: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-indexing-worker:                                    # Phase 1.11
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-indexing-worker
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-indexing-worker/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
      elasticsearch: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  gls-audit-collector:                                    # Phase 1.12
    container_name: ${COMPOSE_PROJECT_NAME:-gls}-audit-collector
    restart: unless-stopped
    build:
      context: .
      dockerfile: backend/gls-audit-collector/Dockerfile
    environment:
      SPRING_PROFILES_ACTIVE: docker
      SPRING_DATA_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200
      RABBITMQ_HOST: rabbitmq
      RABBITMQ_PORT: 5672
      # Tier 2 backend (Phase 1.12 PR3): `mongo` (default) or `es`.
      # Flip to `es` once the audit_tier2_events ES index is wired
      # for ILM in production. Mongo remains the safe default until
      # ES indexing is observed.
      GLS_AUDIT_TIER2_BACKEND: ${GLS_AUDIT_TIER2_BACKEND:-mongo}
    depends_on:
      mongo: { condition: service_healthy }
      rabbitmq: { condition: service_healthy }
    logging: *default-logging
    networks: [gls]

  # (gls-classifier-router placeholder removed — active block is below.)

  # gls-audit-collector:                                  # Phase 1.12 / 0.7 follow-up
  #   container_name: ${COMPOSE_PROJECT_NAME:-gls}-audit-collector
  #   restart: unless-stopped
  #   build:
  #     context: ./backend
  #     dockerfile: gls-audit-collector/Dockerfile
  #   environment:
  #     SPRING_PROFILES_ACTIVE: docker
  #     SPRING_MONGODB_URI: mongodb://root:${MONGO_PASSWORD:-example}@mongo:27017/${MONGO_DB_NAME:-governance_led_storage_main}?authSource=admin
  #     RABBITMQ_HOST: rabbitmq
  #     # Tier 1 / Tier 2 backends are wired here when 0.7 / 1.12 land.
  #   depends_on:
  #     mongo: { condition: service_healthy }
  #     rabbitmq: { condition: service_healthy }
  #   logging: *default-logging
  #   networks: [gls]

volumes:
  grafana_data:

networks:
  gls:
    driver: bridge
